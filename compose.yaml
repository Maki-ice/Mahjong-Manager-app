services:
    app:
        build: .
        ports:
            - "3000:3000"
        volumes:
            - .:/app
            - /app/node_modules
        environment:
            - WATCHPACK_POLLING=true
            - NODE_ENV=development
            - NEXT_TURBOPACK=false
            - CHOKIDAR_USEPOLLING=true